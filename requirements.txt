# Reproducible baseline environment.
#
# PyTorch Geometric extension wheels are platform/CUDA specific. If pip tries
# to build torch-scatter or torch-sparse from source, install the matching wheel
# index first, for example:
#   pip install torch-scatter==2.1.2 torch-sparse==0.6.18 \
#     -f https://data.pyg.org/whl/torch-2.4.1+cpu.html
# For CUDA builds, replace +cpu with the matching CUDA suffix from
# https://data.pyg.org/whl/.

# Core
numpy==1.26.4
pandas==2.2.2
pyyaml==6.0.2
omegaconf==2.3.0
tabulate==0.9.0
tqdm==4.66.5
python-dotenv==1.0.1

# Training / FL runtime
torch==2.4.1
ray==2.35.0

# Graph ML
torch-geometric==2.6.1
torch-scatter==2.1.2
torch-sparse==0.6.18
ogb==1.3.6

# Experiment tracking (optional via config)
wandb==0.17.8

# Common utilities
scikit-learn==1.5.2
pytest==8.3.5
