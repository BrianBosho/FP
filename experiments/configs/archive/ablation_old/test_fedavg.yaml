# Test: aggregation="fedavg_weighted", everything else identical to test_mean.yaml

datasets:
   - Cora
num_clients: [10]
num_rounds: 200
beta: [10000]
lr: 0.5
optimizer: SGD
data_loading: [full]
models: [GAT]
hop: 2
use_pe: [False]

aggregation: "fedavg_weighted"

results_dir: experiments/results/ablation_aggregation
save_results: true
use_wandb: false
