num_clients: 10
num_rounds: 5
epochs: 3
beta: 10000
lr: 0.5
paths:
  datasets_dir: "datasets"
  planetoid:
    cora: "datasets/cora"
    citeseer: "datasets/citeseer"
    pubmed: "datasets/pubmed"
  ogbn:
    arxiv: "datasets/ogbn_arxiv"